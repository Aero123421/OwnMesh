name: CI source snapshot

on:
  push:
    branches:
      - fix/resolve-open-issues-v1.2.17
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Package source tree
        shell: bash
        run: |
          set -euo pipefail
          tar --exclude=.git -czf "$RUNNER_TEMP/ownmesh-ci-source.tar.gz" .
      - name: Upload source tree
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ownmesh-ci-source-${{ github.sha }}
          path: ${{ runner.temp }}/ownmesh-ci-source.tar.gz
          if-no-files-found: error
          retention-days: 1
