name: bad-pin-missing
on: push
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - name: Setup Node
        uses: actions/setup-node
        with:
          node-version: "22"
