{
  "schema_version": 2,
  "release_train": "1.2.0-beta.6",
  "completeness_claim": false,
  "supported": [
    "status",
    "setup (TTY wizard + non-interactive flags/JSON; privacy defaults OFF)",
    "login/logout",
    "doctor (read-only structured diagnostics; --json; network opt-in or when configured)",
    "lockdown/unlock/token revoke",
    "config validate",
    "device enroll/list/show/rotate-key/revoke",
    "exec (local device only)",
    "session list/show/open/attach/claim/release/give/close/terminate (local device only)",
    "approval list/show/approve/deny",
    "policy show/preset/validate/explain",
    "service install/start/stop/restart/status/uninstall (user-level ownmeshd autostart only)",
    "privileged broker status (fail-closed metadata; native service management unsupported)",
    "update check/download/apply/channel (signed GitHub releases; network off by default)"
  ],
  "explicit_unsupported_count": 32,
  "explicit_unsupported_surfaces": [
    "tui",
    "completion",
    "config get",
    "config set",
    "config edit",
    "instance add",
    "instance list",
    "instance use",
    "instance remove",
    "workspace add",
    "workspace list",
    "workspace show",
    "workspace update",
    "workspace remove",
    "process start",
    "process status",
    "process logs",
    "process stop",
    "profile scan",
    "profile list",
    "profile show",
    "profile login",
    "profile test",
    "profile start",
    "profile resume",
    "approval watch",
    "transfer plan",
    "transfer send",
    "transfer list",
    "transfer status",
    "transfer cancel",
    "mcp serve"
  ],
  "additional_unsupported": [
    "device rename",
    "device labels",
    "exec --device",
    "session open <device>",
    "policy rule mutation",
    "privileged broker install",
    "privileged broker uninstall"
  ],
  "total_unsupported_surfaces": 39,
  "notes": "The Rust EXPLICIT_UNSUPPORTED_CLI_SURFACES and ADDITIONAL_UNSUPPORTED_CLI_SURFACES registries are authoritative. Every unsupported entry returns a machine-visible hard error; unregistered command strings are rejected at runtime. Parser, schema, golden-fixture, transport, marker-file, or generated-template presence is not treated as product implementation. v1.2.0-beta.6 hardens E2/E3 durable integrity: pending dispatch outboxes are never wiped by data_json truncation (separate 900 KiB outbox ceiling; oversized claims fail closed); durable result bounding preserves next_offset/sha256/exit_code/list cursors; per-hop read/output budgets align with the 256 KiB durable store and Agent envelope (512 KiB+ files page via offset); directory list enforces a UTF-8 page-byte budget with stable (name,path) cursors; cancel uses a durable target-bound claim+outbox (cancel:<op>) with confirmed-route-only cancel_requested. The scripts/tests/test_v12_e2_e9_workerd_loopback.py gate stays RED until each E4–E9 real binary×workerd row is proven (E2/E3 partial proof is necessary but not sufficient). E2 workerd path covers write/read/command+env, resume, idempotency, cancel, binary range, 512 KiB multi-chunk read, list/stat/delete. Workspace CRUD, cloud PTY sessions, nine profile adapters, bounded unified-diff patch + Git review, elevated broker mint, and transfer remain unsupported. Live-account E2E (E10) remains open. Privileged broker install/uninstall stay side-effect-free unsupported until secure mint authority exists."
}
