{
  "schema_version": 2,
  "release_train": "1.2.0-beta.7",
  "completeness_claim": false,
  "supported": [
    "status",
    "setup (TTY wizard + non-interactive flags/JSON; privacy defaults OFF)",
    "login/logout",
    "doctor (read-only structured diagnostics; --json; network opt-in or when configured)",
    "lockdown/unlock/token revoke",
    "config validate",
    "device enroll/list/show/rotate-key/revoke",
    "exec (local device only)",
    "session list/show/open/attach/claim/release/give/close/terminate (local device only)",
    "approval list/show/approve/deny",
    "policy show/preset/validate/explain",
    "service install/start/stop/restart/status/uninstall (user-level ownmeshd autostart only)",
    "privileged broker status (fail-closed metadata; native service management unsupported)",
    "update check/download/apply/channel (signed GitHub releases; network off by default)"
  ],
  "explicit_unsupported_count": 32,
  "explicit_unsupported_surfaces": [
    "tui",
    "completion",
    "config get",
    "config set",
    "config edit",
    "instance add",
    "instance list",
    "instance use",
    "instance remove",
    "workspace add",
    "workspace list",
    "workspace show",
    "workspace update",
    "workspace remove",
    "process start",
    "process status",
    "process logs",
    "process stop",
    "profile scan",
    "profile list",
    "profile show",
    "profile login",
    "profile test",
    "profile start",
    "profile resume",
    "approval watch",
    "transfer plan",
    "transfer send",
    "transfer list",
    "transfer status",
    "transfer cancel",
    "mcp serve"
  ],
  "additional_unsupported": [
    "device rename",
    "device labels",
    "exec --device",
    "session open <device>",
    "policy rule mutation",
    "privileged broker install",
    "privileged broker uninstall"
  ],
  "total_unsupported_surfaces": 39,
  "notes": "The Rust EXPLICIT_UNSUPPORTED_CLI_SURFACES and ADDITIONAL_UNSUPPORTED_CLI_SURFACES registries are authoritative. Every unsupported entry returns a machine-visible hard error; unregistered command strings are rejected at runtime. Parser, schema, golden-fixture, transport, marker-file, or generated-template presence is not treated as product implementation. v1.2.0-beta.7 advances E2–E5/E7 foundations without promoting incomplete CLI surfaces: directory list is cursor-resumable past 4k+ entries (no silent drop after the old scan_budget window); restricted custody rejects multi-link hardlinks and cross-volume mounts after handle final-path checks; ownmeshd loads state_dir/workspaces.json and selects roots by workspace_id (ws_...) at the FS/git side-effect boundary; public MCP proves patch, raw shell, workspace selection/cross-denial, and session.open metadata/lease; Agent maps session.* and git.status/diff; git stdout/stderr are streamed with hard caps (no Command::output unbounded collect). CLI workspace/profile/transfer CRUD and privileged broker install remain explicit unsupported. The scripts/tests/test_v12_e2_e9_workerd_loopback.py gate stays RED (exit 2) until each E4–E9 acceptance row is fully evidenced — partial E2/E3/E4/E5/E7 proof is necessary but not sufficient. E6 profile adapters, E8 elevated broker mint, E9 resumable transfer, and live-account E10 remain open."
}
