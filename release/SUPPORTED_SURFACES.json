{
  "schema_version": 2,
  "release_train": "1.2.0-beta.7",
  "completeness_claim": false,
  "supported": [
    "status",
    "setup (TTY wizard + non-interactive flags/JSON; privacy defaults OFF)",
    "login/logout",
    "doctor (read-only structured diagnostics; --json; network opt-in or when configured)",
    "lockdown/unlock/token revoke",
    "config validate",
    "device enroll/list/show/rotate-key/revoke",
    "exec (local device only)",
    "session list/show/open/attach/claim/release/give/close/terminate (local device only)",
    "approval list/show/approve/deny",
    "policy show/preset/validate/explain",
    "service install/start/stop/restart/status/uninstall (user-level ownmeshd autostart only)",
    "privileged broker status (fail-closed metadata; native service management unsupported)",
    "update check/download/apply/channel (signed GitHub releases; network off by default)"
  ],
  "explicit_unsupported_count": 32,
  "explicit_unsupported_surfaces": [
    "tui",
    "completion",
    "config get",
    "config set",
    "config edit",
    "instance add",
    "instance list",
    "instance use",
    "instance remove",
    "workspace add",
    "workspace list",
    "workspace show",
    "workspace update",
    "workspace remove",
    "process start",
    "process status",
    "process logs",
    "process stop",
    "profile scan",
    "profile list",
    "profile show",
    "profile login",
    "profile test",
    "profile start",
    "profile resume",
    "approval watch",
    "transfer plan",
    "transfer send",
    "transfer list",
    "transfer status",
    "transfer cancel",
    "mcp serve"
  ],
  "additional_unsupported": [
    "device rename",
    "device labels",
    "exec --device",
    "session open <device>",
    "policy rule mutation",
    "privileged broker install",
    "privileged broker uninstall"
  ],
  "total_unsupported_surfaces": 39,
  "notes": "v1.2.0-beta.7 integrity visit: directory list materializes a full bounded snapshot then sorts before any cursor (fail-closed past 25k entries) so unordered FS enumeration cannot permanently skip early names; git status exposes truncated and never claims exhausted/clean on a capped porcelain capture; git diff pages a durable line spool for forward-progress cursors; session.open persists workspace_id and attach/write/resize reject mismatches; SERVICE_VERSION aligned to 1.2.0-beta.7 with release-checker enforcement. Restricted presets still fail-closed on command.run until OS confinement; session.attach role=observer demotes controller; OAuth AS metadata omits registration_endpoint unless ALLOW_DYNAMIC_CLIENT_REGISTRATION=true. The Rust EXPLICIT_UNSUPPORTED_CLI_SURFACES and ADDITIONAL_UNSUPPORTED_CLI_SURFACES registries are authoritative. Parser/schema/marker presence is not product implementation. CLI workspace/profile/transfer CRUD and privileged broker install remain explicit unsupported. The scripts/tests/test_v12_e2_e9_workerd_loopback.py gate stays RED (exit 2) until each E4\u2013E9 acceptance row is fully evidenced. E5 live PTY host, E6 profile adapters, E7 unified-diff review, E8 elevated broker mint, E9 resumable transfer, and live-account E10 remain open."
}
