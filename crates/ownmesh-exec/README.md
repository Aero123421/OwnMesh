# ownmesh-exec

Structured command execution, raw shell, and process lifecycle.

Structured executable pinning, revalidation, and privileged staging stream
SHA-256 with a shared 512 MiB hard limit. This admits ordinary large developer
tools while preventing unbounded executable reads or staging.
