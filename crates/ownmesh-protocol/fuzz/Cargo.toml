[package]
name = "ownmesh-protocol-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
ownmesh-protocol = { path = ".." }

[[bin]]
name = "parse_envelope"
path = "fuzz_targets/parse_envelope.rs"
doc = false
test = false
bench = false
