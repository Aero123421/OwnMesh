{
  "name": "@ownmesh/control-plane",
  "private": true,
  "version": "1.2.0-beta.7",
  "description": "OwnMesh Cloudflare Workers control plane",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "node --experimental-strip-types --experimental-sqlite --test src/**/*.test.ts",
    "dev": "wrangler dev",
    "db:migrations:apply": "wrangler d1 migrations apply DB --remote",
    "db:migrations:apply:local": "wrangler d1 migrations apply DB --local",
    "deploy": "wrangler d1 migrations apply DB --remote && wrangler deploy"
  },
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "D1 database for OAuth tokens, devices, grants, and audit metadata (auto-provisioned)."
      },
      "DEVICE_ROOM": {
        "description": "Durable Object namespace for per-device hibernatable WebSocket rooms."
      },
      "OAUTH_ISSUER": {
        "description": "Optional canonical issuer URL. Defaults to the Worker request origin."
      },
      "SESSION_SECRET": {
        "description": "Optional signing secret. Generate with `openssl rand -hex 32`."
      },
      "AUTH_PROVIDER": {
        "description": "Required production service binding that authenticates browser requests and returns principal_id + tenant_id. OAuth browser routes return 503 when absent."
      },
      "OWNMESH_DEV_AUTH_BYPASS": {
        "description": "Explicit local/test-only bypass. Must remain false in production."
      }
    }
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "wrangler": "^4.116.0"
  }
}
