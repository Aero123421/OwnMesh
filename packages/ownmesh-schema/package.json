{
  "name": "@ownmesh/schema",
  "private": true,
  "version": "1.2.33",
  "description": "OwnMesh TypeScript domain types, protocol envelope helpers, and fixture validation",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "node ../../scripts/lint-ts.mjs src",
    "test": "node --experimental-strip-types --test src/fixtures-roundtrip.test.ts src/errors.test.ts src/schema-files.test.ts",
    "_comment": "test stays unit-only; typecheck runs once via pnpm -r typecheck in scripts/ci/suites.toml [typescript] (single source, no duplication)"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.7.2"
  }
}
