{
  "name": "@ownmesh/schema",
  "private": true,
  "version": "1.2.2",
  "description": "OwnMesh TypeScript domain types, protocol envelope helpers, and fixture validation",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "tsc --noEmit && node --experimental-strip-types --test src/fixtures-roundtrip.test.ts src/errors.test.ts src/schema-files.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^7.0.2"
  }
}
