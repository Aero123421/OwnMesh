# OwnMesh v1.2 E4 — workspace custody (partial)

## Status

**Partial foundation only.** Restricted-mode filesystem side effects now use
handle-rooted custody. Workspace CRUD CLI surfaces, multi-workspace product
configuration, and a dedicated real binary × workerd E4 acceptance row remain
**open**. The E2–E9 gate stays red.

## What is implemented

In `ownmesh-fs`, when `WorkspaceRoot` is constructed with `enforce=true`
(`workspace_only` / `recommended` presets):

1. Relative paths are walked as normal components (no absolute / prefix /
   over-climbing `..`).
2. Intermediate directories are opened with no-follow / open-reparse flags and
   must not be symlink or reparse points.
3. The target (or parent for create/write) is opened, then the **opened handle's
   final path** is revalidated against the workspace root
   (`GetFinalPathNameByHandle` on Windows, `/proc/self/fd` on Linux, `F_GETPATH`
   on other Unix) **before** any content is returned or committed.
4. A racing replacement of a regular file with a symlink/junction to an outside
   path fails closed (`SymlinkOrReparse` / `EscapesWorkspace`) and never returns
   outside bytes.
5. list / stat / read-range / write (temp+rename) / delete / hash-checked patch
   and git cwd resolution share this custody path.

Full Access / full_user_access (`enforce=false`) keep workspace root as
context/cwd metadata without a hidden path deny; handle final-path pinning is
not required for unrestricted modes.

## Durable local state bounds (related integrity)

`ownmeshd` loads `grants.json`, `approvals.json`, and `revoked-clients.json`
with stat-before-read, file-byte ceilings, and entry/value budgets. Oversized
or corrupt state fails daemon open rather than allocating unbounded input.

## What is intentionally not claimed

| Surface | Status |
| --- | --- |
| CLI `workspace add/list/show/update/remove` | Explicit unsupported |
| Multi-workspace CRUD + MCP `workspace_id` selection product | Open |
| Hardlink / mount / case-alias matrix on every platform | Partial (symlink/reparse covered; broader matrix still open) |
| Real binary × workerd E4 acceptance row | Open (gate remains red) |
| openat(2)-style directory-fd walk on every hop | Partial (final-handle path revalidation is the authority check) |

## Tests

- `crates/ownmesh-fs` unit + `tests/security_path.rs` (restricted symlink
  non-follow / replacement fail-closed where the platform can create links)
- `ownmeshd` daemon tests: oversized/corrupt grants, approvals, revoked state
  fail `DaemonRuntime::open`

## Next E4 work

1. Durable workspace registry (id, root, owner/tenant binding) with CRUD.
2. Propagate selected `workspace_id` into runtime root selection (not only
   audit metadata) for restricted modes.
3. Real public MCP → workerd → Agent → ownmeshd proof that a restricted
   workspace rejects symlink/junction escape after concurrent replacement.
4. Promote registry surfaces only after that proof; never on parsers alone.
